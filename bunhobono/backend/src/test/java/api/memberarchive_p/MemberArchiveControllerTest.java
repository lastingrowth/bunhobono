package api.memberarchive_p; import org.junit.jupiter.api.*; import org.junit.jupiter.api.extension.ExtendWith; import org.mockito.*; import org.mockito.junit.jupiter.MockitoExtension; import java.util.List; import static org.assertj.core.api.Assertions.*; import static org.mockito.Mockito.*;
@ExtendWith(MockitoExtension.class) class MemberArchiveControllerTest { @Mock MemberArchiveService service; @InjectMocks MemberArchiveController controller; @Test @DisplayName("UT-BE-MEMBERARCHIVE-CTRL-001 전출 이력을 조회한다") void list(){ List<MemberArchiveDTO>x=List.of(); when(service.list()).thenReturn(x); assertThat(controller.list()).isSameAs(x); } @Test @DisplayName("UT-BE-MEMBERARCHIVE-CTRL-002 전출 이력을 삭제한다") void delete(){ controller.delete(1); verify(service).delete(1); } }
